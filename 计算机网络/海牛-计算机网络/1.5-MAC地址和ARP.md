以太网 Ethernet ：以MAC地址互联
因特网 Internet ：以IP地址互联
泛指 互联网 internet

类unix系统：Linux Android IOS MacOS

-----------------------------------------

二、MAC地址

1、MAC地址的==格式特征==

-每一个网卡都有一个6字节（48bit）的MAC地址（Media Access Control Address），MAC地址的结构如下：
![[Pasted image 20260705155236.png]]

-全球唯一，固化在网卡的ROM中，由IEEE802标准规定
	前3字节：OUI，组织唯一标识符，由IEEE的注册管理机构分配给厂商，如Apple
	后3字节：网络接口标识符，由厂商自行分配


![[Pasted image 20260705155444.png]]

2、MAC地址的==获取==

-当不知道对方主机的MAC地址时，可以通过发送ARP广播获取对方的MAC地址
	-获取成功后，会缓存IP地址、MAC地址的映射信息，俗称：ARP缓存
	-通过ARP广播获取的MAC地址，属于动态（dynamic）缓存
		存储时间比较短(默认是2分钟)，过期就自动删除

-相关命令
	![[Pasted image 20260705161145.png]]

3、ARP是什么

ARP协议的全称是 **Address Resolution Protocol** (地址解析协议), 它是一个通过用于**实际从IP地址到MAC地址的映射**，即询问目标IP对应的MAC地址的一种协议。ARP协议在IPv4中极其重要。

下面我们探讨一下ARP的工作机制是怎样的。假设A和B位于同一链路，不需要经过路由器的转换，主机A向主机B发送一个IP分组，主机A的地址是192.168.1.2，主机B的地址是192.168.1.3，他们都不知道对方的MAC地址是啥，主机C和主机D是同一链路的其他主机。

![[Pasted image 20260705164755.png]]

主机A 想要获取主机B 的MAC地址，主机A 会通过 ==广播== 的方式向以太网上所有主机发送一个==ARP 请求包==，这个ARP请求包中包含了主机A 想要知道主机B 的IP地址的MAC地址。

主机A 发送的 ARP请求包 会被同一链路上的所有主机/路由器 接收并进行解析。每个主机/路由器都会检查ARP请求包中的信息，如果ARP请求包 中的 ==目标IP地址== 和自己的相同，就会将自己主机的 MAC地址写入响应包返回主机A。

![[Pasted image 20260705165148.png]]

由此，可以通过ARP从IP地址获取MAC地址，实现同一链路内的通信。

抽象过程：
	![[Pasted image 20260705165244.png]]
	请求包里包括：源IP地址 目标IP地址 源MAC地址 目标MAC地址（不知道，全部用1表示）
	响应包：源MAC地址 目标MAC地址

4、

主机A收到响应包后会暂时存储响应的MAC地址，这样就建立了连接，
但ARP响应获取的MAC地址，只会被保存一定时间，默认是2分钟

![[Pasted image 20260705165446.png]]

5、==RARP==

与 ARP 相对的，RARP（==Reverse Address Resolution Protocol==）是将ARP反过来，从MAC地址定位IP地址的一种协议，将打印服务器等小型嵌入式设备接入网络时会使用的。

![[Pasted image 20260705170056.png]]

目前被BOOTP、DHCP所取代。
