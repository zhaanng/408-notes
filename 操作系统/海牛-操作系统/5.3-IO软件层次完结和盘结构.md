### I/O层次结构

用户级别的I/O软件
设备独立性操作系统软件
设备驱动程序
中断处理程序
硬件（设备控制器硬件 + IO设备硬件）

设备独立性软件：
	设备无关性、大管家、隔离层，
	认识所有硬件，让上层软件不需要去区分硬件，专心只写一种代码即可
设备驱动程序：
	翻译官，
	把无关层发出的抽象命令翻译成硬件指令，明确每个硬件该做的工作
中断处理程序：
	通知员，
	CPU是并发的，随时可能都在处理数据，当软件需要CPU时，
	由它去叫CPU，使CPU被动中断，
	等做完你这个I/O任务后，CPU又会离开去做其他的任务。

驱动程序负责把指令翻译成硬件指令，给每个硬件分配工作，让硬件处于能够被CPU执行的环境中，这一步属于硬件准备工作。
然后准备好后，控制器硬件发出中断电信号，让CPU执行中断处理程序，然后过来执行我这一部分数据。
CPU执行完后，把处理好的数据存放到控制器硬件中，CPU去做其他的事，再由控制器把数据上传。
有很多错误，但大概是这么个意思，先这么理解。。
#### 一、中断处理程序
ISR, Interrupt Service Routines
顶半部 和 底半部
![[Pasted image 20260903094658.png]]

**中断处理方案**：
1、非嵌套  Non-nested 指的是CPU在处理中断时，不会再触发中断处理程序，等到处理完一个后再去处理下一个
2、嵌套   Nested  在A处理过程中去响应另一个更高优先级B的中断。
3、可重入  Re-entrant 处理A中断时去响应另一个A中断
4、简单优先级 Simple Priority 适用于只需要区分少数几个优先等级的系统，实现基本的优先级管理
5、标准优先级 Standard Priority 
![[Pasted image 20260903102725.png]]

#### 中断处理程序的一般过程：
只是一种理论介绍，具体实现要看芯片厂商
这个图的步骤先后有问题，不要记这个顺序
![[Pasted image 20260903110303.png]]
![[Pasted image 20260903110312.png]]
![[Pasted image 20260903110319.png]]
![[Pasted image 20260903110437.png]]
![[Pasted image 20260903110443.png]]

#### 二、设备驱动程序
![[Pasted image 20260903111257.png]]
![[Pasted image 20260903111302.png]]

#### 三、与设备无关的I/O软件
![[Pasted image 20260903111947.png]]
![[Pasted image 20260903112449.png]]

#### 四、用户空间的I/O软件

----------
### 盘

#### 盘硬件
硬盘 hard disk,HDD
非易失性存储设备
支持随机访问，即按照任意顺序存储和检索数据
![[Pasted image 20260903122323.png]]
![[Pasted image 20260903122438.png]]

围成扇区的是扇道，扇区是那个弧形区域，外面两条线是扇道

1、每个扇区都是存储512B，因为是同心圆，且认为是从圆心往四周按相同大小的弧度来划分扇区，但距圆心距离不同，导致扇区面积并不是都相等。
所以近心端的扇区的密度大，远心端的扇区密度小
这些都是老式磁盘

2、现代有很多磁盘，是保证扇区的微密度相等，越远离圆心，圆弧上划分的扇区就越多，保证每个扇区存储的数据都一样大，512B

但在学习中，为了简化模型，认为只有第1种磁盘

![[Pasted image 20260903124023.png]]

#### RAID
![[Pasted image 20260903123957.png]]


外存类型：
一、机械类磁盘 磁表面存储器
1、硬盘 HDD，机械硬盘
2、软盘
3、磁带

二、闪存/固态存储 Flash，非机械
1、SSD固态硬盘
2、U盘 - USB闪存盘
3、SD卡、TF卡、存储卡

三、光盘存储器 光存储
CD-ROM、DVD、蓝光光盘
